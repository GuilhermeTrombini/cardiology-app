import { StyleSheet } from "react-native";

export const getStyles = () =>
  StyleSheet.create({
    defaultText: {
      fontSize: 12,
      fontWeight: "400",
      fontFamily: "SFProDisplay-Regular",
    },
  });
